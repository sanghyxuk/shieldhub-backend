
-----

# 🛡️ ShieldHub Backend

[![Java 17](https://img.shields.io/badge/Java-17-orange.svg)](https://www.oracle.com/java/technologies/javase/jdk17-archive-downloads.html)
[![Spring Boot](https://img.shields.io/badge/Spring%20Boot-3.5.5-brightgreen.svg)](https://spring.io/projects/spring-boot)
[![MySQL](https://img.shields.io/badge/MySQL-8.0-blue.svg)](https://www.mysql.com/)
[![Docker](https://img.shields.io/badge/Docker-Ready-blue.svg)](https://www.docker.com/)

> 파일 보안과 웹 취약점 분석을 제공하는 통합 보안 솔루션 백엔드 서버

<br>

## 📚 목차

1.  [프로젝트 소개](#-프로젝트-소개)
2.  [주요 기능](#-주요-기능)
3.  [기술 스택](#️-기술-스택)
4.  [시스템 아키텍처](#-시스템-아키텍처)
5.  [빠른 시작](#-빠른-시작)
6.  [API 문서](#-api-문서)
7.  [프로젝트 구조](#-프로젝트-구조)
8.  [보안 기능](#-보안-기능)
9.  [데이터베이스 스키마](#️-데이터베이스-스키마)
10. [개발 진행 상황](#-개발-진행-상황)
11. [테스트 가이드](#-테스트-가이드)
12. [주의사항](#️-주의사항)

<br>

## 📌 프로젝트 소개

**ShieldHub**는 민감한 파일을 안전하게 보호하고 웹사이트의 보안 취약점을 분석하는 통합 보안 솔루션 백엔드 시스템입니다. AES-256-GCM 이중 암호화 시스템을 통해 강력한 파일 보안을 제공하며, JWT와 OTP/2FA를 결합한 다중 인증 체계로 사용자 계정을 안전하게 보호합니다. 또한 Flask AI 서버와 연동하여 머신러닝 기반의 웹 취약점 자동 탐지 기능을 제공합니다.

### ✨ 핵심 특징

* 🔐 **강력한 파일 암호화**: 파일별 고유 키와 마스터 키를 사용하는 **AES-256-GCM 이중 암호화** 시스템
* 🛡️ **다중 인증 시스템**: **JWT** 토큰 인증과 **OTP/2FA**를 결합하여 높은 수준의 계정 보안 제공
* ✅ **무결성 보장**: **SHA-256** 해시 검증을 통해 파일의 위변조 여부를 확인
* 🔍 **AI 기반 취약점 분석**: Flask 머신러닝 서버와 연동하여 웹사이트 보안 취약점 자동 탐지
* 👤 **사용자별 격리**: 모든 파일과 분석 데이터는 사용자별로 완벽히 격리되어 관리
* 📧 **이메일 연동**: Gmail SMTP를 통한 비밀번호 재설정 및 알림 기능

-----

## 🚀 주요 기능

### 🔐 보안 인증 시스템

* ✅ JWT 기반 토큰 인증 (Access Token)
* ✅ BCrypt를 사용한 비밀번호 단방향 암호화
* ✅ Google Authenticator 연동 OTP/2FA 2단계 인증
* ✅ 회원가입, 로그인 (일반/OTP), 로그아웃 기능
* ✅ 아이디 찾기 (이름 + 이메일)
* ✅ 안전한 비밀번호 변경 및 재설정
* ✅ 이메일 기반 임시 비밀번호 발급 (Gmail SMTP)
* ✅ 회원 탈퇴 기능

### 📁 파일 보안 시스템

* ✅ **AES-256-GCM**을 사용한 강력한 파일 암호화/복호화
* ✅ 파일 키 + 마스터 키를 이용한 **이중 암호화**로 키 관리 보안 강화
* ✅ **SHA-256** 해시를 이용한 파일 무결성 검증
* ✅ 암호화된 파일과 키, 정보를 하나의 **ZIP**으로 패키징
* ✅ 파일 암호화/복호화 이력 자동 기록 (FileHistory)
* ✅ 사용자별 파일 저장소 완전 격리
* ✅ 업로드 방식 및 DB 참조 방식 복호화 지원
* ✅ 사용자 파일 목록 조회 기능

### 🔍 웹 취약점 분석 시스템

* ✅ Flask AI 서버와 RestTemplate 기반 연동
* ✅ URL 기반 웹사이트 보안 취약점 자동 탐지
* ✅ SQL Injection, XSS, CSRF 등 주요 취약점 탐지
* ✅ 취약점 위험도(HIGH/MEDIUM/LOW) 자동 분류
* ✅ 분석 결과 DB 자동 저장 및 이력 관리
* ✅ 사용자별 분석 결과 격리 저장

-----

## 🛠️ 기술 스택

| 구분                  | 기술                                                                                              |
| --------------------- | ------------------------------------------------------------------------------------------------- |
| **Language** | `Java 17`                                                                                          |
| **Framework** | `Spring Boot 3.5.5`, `Spring Security 6.x`, `Spring Data JPA`, `Spring Validation`                |
| **Database** | `MySQL 8.0`, `Hibernate`                                                                           |
| **Security** | `AES-256-GCM`, `SHA-256`, `BCrypt`, `JWT (jjwt 0.11.5)`                                           |
| **Authentication** | `Google Authenticator (TOTP)`, `ZXing QR Code`                                                    |
| **Email** | `Spring Mail`, `Gmail SMTP`                                                                        |
| **AI Integration** | `RestTemplate`, `Flask API Server (http://127.0.0.1:5001)`                                        |
| **Build Tool** | `Maven`                                                                                            |
| **DevOps** | `Docker`, `Docker Compose`                                                                         |
| **Utility** | `Lombok`, `Spring Boot DevTools`, `Spring Boot Actuator`                                          |

-----

## 🏗 시스템 아키텍처

```
┌─────────────────┐
│   Frontend      │
│   (React)       │
└────────┬────────┘
         │ HTTP/REST API
         ▼
┌─────────────────────────────────────────────────┐
│         ShieldHub Backend (Spring Boot)         │
│  ┌──────────────────────────────────────────┐  │
│  │   Security Layer (JWT + OTP Filter)      │  │
│  └──────────────────────────────────────────┘  │
│  ┌──────────────┬────────────┬──────────────┐  │
│  │ Auth Service │File Service│Analysis Svc  │  │
│  └──────────────┴────────────┴──────────────┘  │
└────────┬──────────────────────────┬─────────────┘
         │                          │
         ▼                          ▼
┌──────────────────┐     ┌──────────────────────┐
│   MySQL 8.0      │     │  Flask AI Server     │
│                  │     │  (ML Vulnerability   │
│  - Users         │     │   Detection)         │
│  - FileMetadata  │     │  Port: 5001          │
│  - AnalysisRes.. │     └──────────────────────┘
│  - Vulnerabil..  │
└──────────────────┘
```

### 주요 컴포넌트

- **Spring Security**: JWT 인증 필터 + OTP 검증
- **FileEncryptionService**: AES-256-GCM 이중 암호화
- **AnalysisService**: Flask 서버 연동 및 결과 처리
- **EmailService**: Gmail SMTP를 통한 이메일 발송

-----

## 🏁 빠른 시작

### 1\. 환경 요구사항

* Java 17 이상
* Docker & Docker Compose
* Maven 3.6 이상

### 2\. 프로젝트 클론

```bash
git clone https://github.com/yourusername/shieldhub-backend.git
cd shieldhub-backend
```

### 3\. 환경 설정

`application-example.yml` 파일을 복사하여 `application.yml` 파일을 생성하고, 주요 설정값을 본인 환경에 맞게 수정합니다.

```bash
cp src/main/resources/application-example.yml src/main/resources/application.yml
```

- **수정 항목:**
   - `app.jwt.secret`: 256비트(32바이트) JWT 시크릿 키
   - `app.encryption.master-key`: 256비트(32바이트) 암호화 마스터 키
   - `spring.datasource.password`: 데이터베이스 비밀번호

### 4\. Docker 컨테이너 실행

`docker-compose.yml` 파일이 있는 프로젝트 루트에서 아래 명령어를 실행하여 데이터베이스 컨테이너를 시작합니다.

```bash
docker-compose up -d
```

### 5\. 애플리케이션 실행

Maven을 사용하여 프로젝트를 빌드하고 실행합니다.

```bash
mvn clean install
mvn spring-boot:run
```

### 6\. 동작 확인

서버가 정상적으로 실행되었는지 확인합니다.

```bash
curl http://localhost:8080/api/test/health
```

"Backend is running\!" 메시지가 출력되면 성공입니다.

-----

## 📑 API 문서

### 🔐 인증 API (`/api/auth`)

| Method     | Endpoint                    | 설명                      | 인증 필요 |
| :--------- | :-------------------------- | :------------------------ | :-------: |
| **POST**   | `/register`                 | 회원가입                  |     -     |
| **POST**   | `/login`                    | 일반 로그인               |     -     |
| **POST**   | `/login-with-otp`           | OTP 로그인                |     -     |
| **POST**   | `/check-otp`                | OTP 활성화 확인           |     -     |
| **POST**   | `/find-id`                  | 아이디 찾기               |     -     |
| **POST**   | `/logout`                   | 로그아웃                  |     ✅     |
| **PUT**    | `/change-password`          | 비밀번호 변경             |     ✅     |
| **POST**   | `/reset-password`           | 비밀번호 재설정 요청      |     -     |
| **DELETE** | `/delete-account`           | 회원 탈퇴                 |     ✅     |

### 🔑 OTP API (`/api/otp`)

| Method   | Endpoint   | 설명                      | 인증 필요 |
| :------- | :--------- | :------------------------ | :-------: |
| **POST** | `/setup`   | OTP 설정용 QR 코드 생성   |     ✅     |
| **POST** | `/enable`  | OTP 활성화                |     ✅     |
| **POST** | `/disable` | OTP 비활성화              |     ✅     |
| **GET**  | `/status`  | OTP 상태 확인             |     ✅     |

### 📁 파일 보안 API (`/api/files`)

| Method   | Endpoint                | 설명                      | 인증 필요 |
| :------- | :---------------------- | :------------------------ | :-------: |
| **POST** | `/encrypt`              | 파일 암호화 (ZIP 다운로드)|     ✅     |
| **POST** | `/decrypt-upload`       | 파일 복호화 (업로드 방식) |     ✅     |
| **POST** | `/decrypt/{fileId}`     | 파일 복호화 (DB 참조)     |     ✅     |
| **GET**  | `/list`                 | 내 파일 목록 조회         |     ✅     |

### 🔍 웹 취약점 분석 API (`/api/analysis`)

| Method   | Endpoint | 설명                          | 인증 필요 |
| :------- | :------- | :---------------------------- | :-------: |
| **POST** | `/start` | 웹사이트 취약점 분석 시작     |     ✅     |

### 🧪 테스트 API (`/api/test`)

| Method  | Endpoint  | 설명                    | 인증 필요 |
| :------ | :-------- | :---------------------- | :-------: |
| **GET** | `/health` | 서버 상태 확인          |     -     |
| **GET** | `/users`  | 전체 사용자 조회 (개발용)|     -     |

-----

## 📂 프로젝트 구조

```
shieldhub-backend/
│
├── src/main/java/com/shieldhub/backend/
│   ├── ShieldhubBackendApplication.java    # 메인 애플리케이션
│   │
│   ├── config/                              # 설정 클래스
│   │   ├── AppConfig.java                   # RestTemplate 등 빈 설정
│   │   ├── SecurityConfig.java              # Spring Security 설정
│   │   ├── JwtAuthenticationFilter.java     # JWT 인증 필터
│   │   └── WebConfig.java                   # CORS 설정
│   │
│   ├── controller/                          # REST API 컨트롤러
│   │   ├── AuthController.java              # 인증 API (회원가입, 로그인 등)
│   │   ├── OtpController.java               # OTP API
│   │   ├── FileController.java              # 파일 암호화/복호화 API
│   │   ├── AnalysisController.java          # 웹 취약점 분석 API
│   │   └── TestController.java              # 테스트용 API
│   │
│   ├── dto/                                 # 데이터 전송 객체
│   │   ├── request/                         # 요청 DTO
│   │   │   ├── LoginRequest.java
│   │   │   ├── RegisterRequest.java
│   │   │   ├── ChangePasswordRequest.java
│   │   │   ├── OtpVerifyRequest.java
│   │   │   ├── AnalysisRequest.java
│   │   │   └── ...
│   │   └── response/                        # 응답 DTO
│   │       ├── LoginResponse.java
│   │       ├── OtpSetupResponse.java
│   │       ├── FlaskAnalysisResponse.java
│   │       └── ...
│   │
│   ├── entity/                              # JPA 엔티티 (DB 테이블 매핑)
│   │   ├── User.java                        # 사용자 정보
│   │   ├── FileMetadata.java               # 파일 메타데이터
│   │   ├── FileHistory.java                # 파일 이력
│   │   ├── AnalysisResults.java            # 분석 결과
│   │   ├── Vulnerabilities.java            # 취약점 정보
│   │   └── AnalysisVulnerabilities.java    # 분석-취약점 연결 테이블
│   │
│   ├── repository/                          # JPA Repository
│   │   ├── UserRepository.java
│   │   ├── FileMetadataRepository.java
│   │   ├── FileHistoryRepository.java
│   │   ├── AnalysisResultsRepository.java
│   │   ├── VulnerabilitiesRepository.java
│   │   └── AnalysisVulnerabilitiesRepository.java
│   │
│   ├── service/                             # 비즈니스 로직
│   │   ├── AuthService.java                 # 인증 서비스 (로그인, 회원가입 등)
│   │   ├── UserService.java                 # 사용자 관리
│   │   ├── OtpService.java                  # OTP 생성 및 검증
│   │   ├── EmailService.java                # 이메일 발송
│   │   ├── FileEncryptionService.java       # 파일 암호화/복호화
│   │   ├── AnalysisService.java             # 웹 취약점 분석
│   │   └── CustomUserDetailsService.java    # Spring Security UserDetails
│   │
│   └── util/                                # 유틸리티 클래스
│       ├── JwtTokenUtil.java                # JWT 토큰 생성/검증
│       └── EncryptionUtil.java              # AES-256-GCM 암호화
│
├── src/main/resources/
│   ├── application.yml                      # 애플리케이션 설정
│   └── application-example.yml              # 설정 예시 파일
│
├── docker-compose.yml                       # Docker Compose 설정
├── pom.xml                                  # Maven 의존성 관리
├── README.md                                # 프로젝트 문서
└── data/                                    # 암호화된 파일 저장소
    └── files/encrypted/uploads/
```

-----

## 🔐 보안 기능

### 🗄️ 파일 암호화 프로세스

1.  사용자가 파일을 업로드하면 서버에서 바이트 배열로 변환합니다.
2.  원본 파일의 **SHA-256 해시값**을 생성하여 무결성 검증용으로 기록합니다.
3.  암호학적으로 안전한 **랜덤 AES-256 키(파일 키)**를 생성합니다.
4.  **AES-GCM 모드**로 파일을 암호화합니다. (안전한 IV 자동 포함)
5.  암호화된 파일은 UUID 기반의 랜덤 파일명으로 서버에 저장됩니다.
6.  파일 암호화에 사용된 **파일 키**를 미리 정의된 **마스터 키**로 한번 더 암호화합니다.
7.  `암호화된 파일`, `암호화된 키`, `파일 정보`를 하나의 **ZIP 파일**로 패키징하여 사용자에게 전달합니다.
8.  관련 메타데이터(파일명, 해시값 등)를 DB에 저장하고 이력(`FileHistory`)을 기록합니다.

### 🔓 파일 복호화 프로세스

1.  사용자가 암호화된 파일과 키를 업로드하거나 DB에서 파일 ID를 제공합니다.
2.  서버는 **마스터 키**로 암호화된 **파일 키**를 먼저 복호화합니다.
3.  복호화된 **파일 키**로 암호화된 파일을 복호화합니다.
4.  원본 파일의 **SHA-256 해시**를 검증하여 무결성을 확인합니다.
5.  복호화된 파일을 사용자에게 전달하고 이력을 기록합니다.

### 👤 인증 프로세스

#### 일반 로그인
1.  사용자가 ID/PW로 로그인을 요청합니다.
2.  서버는 **BCrypt**로 해시된 비밀번호를 비교하여 사용자를 검증합니다.
3.  검증 성공 시, 사용자의 정보를 담은 **JWT Access Token**을 발급합니다.
4.  클라이언트는 이후 모든 요청의 `Authorization: Bearer {token}` 헤더에 JWT를 담아 전송합니다.
5.  서버의 **JWT 필터**가 토큰을 자동으로 검증하고 사용자를 인증 처리합니다.

#### OTP 로그인 (2단계 인증)
1.  사용자는 먼저 `/api/auth/check-otp`로 OTP 활성화 여부를 확인합니다.
2.  OTP가 활성화된 경우, `/api/auth/login-with-otp`로 ID/PW와 OTP 코드를 함께 전송합니다.
3.  서버는 비밀번호 검증 후, Google Authenticator TOTP 코드를 검증합니다.
4.  모든 검증이 성공하면 JWT 토큰을 발급합니다.

### 🔍 웹 취약점 분석 프로세스

1.  사용자가 분석할 웹사이트 URL을 입력합니다. (`POST /api/analysis/start`)
2.  서버는 `AnalysisResults` 테이블에 **PENDING** 상태로 분석 기록을 생성합니다.
3.  **RestTemplate**을 사용하여 Flask AI 서버(`http://127.0.0.1:5001`)로 URL을 전송합니다.
4.  Flask 서버가 머신러닝 모델로 취약점을 탐지하고 결과를 반환합니다.
5.  탐지된 취약점들을 `Vulnerabilities` 테이블에 저장합니다. (중복 체크)
6.  `AnalysisVulnerabilities` 연결 테이블에 분석 ID와 취약점 ID를 매핑합니다.
7.  분석 상태를 **COMPLETED**로 업데이트하고 결과를 사용자에게 반환합니다.
8.  오류 발생 시 상태를 **FAILED**로 업데이트하고 에러 메시지를 기록합니다.

-----

## 🗃️ 데이터베이스 스키마

### 📊 ERD 구조

```
┌─────────────┐           ┌──────────────────┐           ┌───────────────────┐
│    Users    │           │  FileMetadata    │           │   FileHistory     │
├─────────────┤           ├──────────────────┤           ├───────────────────┤
│ user_id (PK)│◄─────────┤ user_id (FK)     │◄─────────┤ file_id (FK)      │
│ username    │           │ file_id (PK)     │           │ history_id (PK)   │
│ password_hash│          │ file_name        │           │ action_type       │
│ name        │           │ file_path        │           │ timestamp         │
│ email       │           │ file_size        │           └───────────────────┘
│ phone_number│           │ sha256_hash      │
│ otp_secret  │           │ upload_date      │
│ is_otp_enabled│         └──────────────────┘
│ created_at  │
│ updated_at  │           ┌──────────────────┐           ┌───────────────────┐
└─────────────┘           │ AnalysisResults  │           │ Vulnerabilities   │
       │                  ├──────────────────┤           ├───────────────────┤
       │                  │ analysis_id (PK) │           │vulnerability_id(PK)│
       └─────────────────►│ user_id (FK)     │           │vulnerability_type │
                          │ url_to_analyze   │           │ severity          │
                          │ analysis_date    │           │ details           │
                          │ analysis_status  │           │ detected_pattern  │
                          │ completion_date  │           └───────────────────┘
                          │ error_message    │                     ▲
                          └──────────────────┘                     │
                                    │                              │
                                    │    ┌────────────────────────┴──┐
                                    └───►│ AnalysisVulnerabilities   │
                                         ├───────────────────────────┤
                                         │ analysis_id (FK, PK)      │
                                         │ vulnerability_id (FK, PK) │
                                         │ confidence                │
                                         └───────────────────────────┘
```

### 📋 테이블 상세

#### **Users** - 사용자 정보
| 컬럼             | 타입          | 제약조건                | 설명                      |
|-----------------|--------------|------------------------|---------------------------|
| user_id         | INT          | PK, AUTO_INCREMENT     | 사용자 고유 ID            |
| username        | VARCHAR(255) | UNIQUE, NOT NULL       | 로그인 아이디             |
| password_hash   | VARCHAR(255) | NOT NULL               | BCrypt 해시 비밀번호      |
| name            | VARCHAR(100) | NOT NULL               | 사용자 이름               |
| phone_number    | VARCHAR(20)  |                        | 전화번호                  |
| email           | VARCHAR(255) |                        | 이메일 (비밀번호 재설정용)|
| otp_secret      | VARCHAR(255) |                        | OTP 시크릿 키             |
| is_otp_enabled  | BOOLEAN      | DEFAULT FALSE          | OTP 활성화 여부           |
| created_at      | TIMESTAMP    | DEFAULT CURRENT_TIMESTAMP | 가입일                 |
| updated_at      | TIMESTAMP    | ON UPDATE              | 정보 수정일               |

#### **FileMetadata** - 파일 메타데이터
| 컬럼          | 타입          | 제약조건               | 설명                      |
|--------------|--------------|------------------------|---------------------------|
| file_id      | INT          | PK, AUTO_INCREMENT     | 파일 고유 ID              |
| file_name    | VARCHAR(255) | NOT NULL               | 원본 파일명               |
| user_id      | INT          | FK (Users), NOT NULL   | 파일 소유자 ID            |
| file_path    | VARCHAR(500) | NOT NULL               | 암호화된 파일 저장 경로   |
| file_size    | BIGINT       |                        | 파일 크기 (bytes)         |
| sha256_hash  | VARCHAR(64)  | NOT NULL               | 파일 무결성 검증 해시     |
| upload_date  | TIMESTAMP    | DEFAULT CURRENT_TIMESTAMP | 업로드 날짜           |

#### **FileHistory** - 파일 처리 이력
| 컬럼         | 타입       | 제약조건                    | 설명                       |
|-------------|-----------|----------------------------|----------------------------|
| history_id  | INT       | PK, AUTO_INCREMENT         | 이력 고유 ID               |
| file_id     | INT       | FK (FileMetadata), NOT NULL| 파일 ID                    |
| action_type | ENUM      | ENCRYPTION, DECRYPTION     | 작업 유형                  |
| timestamp   | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP  | 작업 시각                  |

#### **AnalysisResults** - 웹 취약점 분석 결과
| 컬럼             | 타입          | 제약조건                 | 설명                       |
|-----------------|--------------|--------------------------|----------------------------|
| analysis_id     | INT          | PK, AUTO_INCREMENT       | 분석 고유 ID               |
| user_id         | INT          | FK (Users)               | 분석 요청 사용자 ID        |
| url_to_analyze  | VARCHAR(500) | NOT NULL                 | 분석 대상 URL              |
| analysis_date   | TIMESTAMP    | DEFAULT CURRENT_TIMESTAMP| 분석 시작 시간             |
| analysis_status | ENUM         | PENDING, IN_PROGRESS, COMPLETED, FAILED | 분석 상태 |
| completion_date | TIMESTAMP    |                          | 분석 완료 시간             |
| error_message   | TEXT         |                          | 오류 메시지 (실패 시)      |

#### **Vulnerabilities** - 취약점 정보
| 컬럼                | 타입          | 제약조건           | 설명                       |
|--------------------|--------------|-------------------|----------------------------|
| vulnerability_id   | INT          | PK, AUTO_INCREMENT| 취약점 고유 ID             |
| vulnerability_type | VARCHAR(100) | NOT NULL          | 취약점 유형 (SQL Injection, XSS 등) |
| severity           | ENUM         | HIGH, MEDIUM, LOW | 위험도                     |
| details            | TEXT         |                   | 취약점 상세 설명           |
| detected_pattern   | TEXT         |                   | 감지된 패턴                |

#### **AnalysisVulnerabilities** - 분석-취약점 연결 테이블 (N:M)
| 컬럼              | 타입    | 제약조건                          | 설명                       |
|------------------|---------|----------------------------------|----------------------------|
| analysis_id      | INT     | FK (AnalysisResults), PK         | 분석 ID                    |
| vulnerability_id | INT     | FK (Vulnerabilities), PK         | 취약점 ID                  |
| confidence       | DECIMAL |                                  | 신뢰도 (0.0 ~ 1.0)         |

-----

## 📈 개발 진행 상황

| 구분             | 기능                                    | 상태 |
| :--------------- | :-------------------------------------- | :--: |
| **인프라**       | Docker 환경 구축 (MySQL)                |  ✅  |
|                  | Spring Boot 프로젝트 설정               |  ✅  |
|                  | DB 스키마 설계 및 구현                  |  ✅  |
| **인증 시스템**  | JWT 기반 토큰 인증 (Spring Security)    |  ✅  |
|                  | 회원가입/로그인/로그아웃                |  ✅  |
|                  | 아이디 찾기 (이름+이메일)               |  ✅  |
|                  | 비밀번호 변경/재설정 (이메일 발송)      |  ✅  |
|                  | 회원 탈퇴                               |  ✅  |
|                  | OTP/2FA (Google Authenticator)          |  ✅  |
| **파일 보안**    | AES-256-GCM 이중 암호화                 |  ✅  |
|                  | SHA-256 무결성 검증                     |  ✅  |
|                  | 파일 이력 자동 기록                     |  ✅  |
|                  | ZIP 패키징 시스템                       |  ✅  |
|                  | 업로드 방식 복호화                      |  ✅  |
|                  | DB 참조 방식 복호화                     |  ✅  |
|                  | 사용자별 파일 목록 조회                 |  ✅  |
| **취약점 분석**  | Flask AI 서버 연동 (RestTemplate)       |  ✅  |
|                  | 웹사이트 취약점 자동 탐지               |  ✅  |
|                  | 분석 결과 DB 저장 및 이력 관리          |  ✅  |
|                  | 취약점 위험도 분류 (HIGH/MEDIUM/LOW)    |  ✅  |
| **이메일 연동**  | Gmail SMTP 설정                         |  ✅  |
|                  | 비밀번호 재설정 이메일 발송             |  ✅  |

### 🎯 향후 개발 계획

- [ ] Redis 캐시 연동 (세션 관리)
- [ ] JWT Refresh Token 구현
- [ ] 파일 업로드 용량 제한 및 검증 강화
- [ ] 분석 결과 조회 API 추가
- [ ] 분석 이력 통계 및 대시보드 기능
- [ ] API 문서 자동화 (Swagger/SpringDoc)
- [ ] 단위 테스트 및 통합 테스트 작성
- [ ] 이메일 인증 시스템 강화
- [ ] 관리자 대시보드

-----

## 🧪 테스트 가이드

### API 테스트 예시

#### 1. 회원가입
```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "user123",
    "name": "테스트 유저",
    "email": "test@example.com"
  }'
```

#### 2. 로그인 및 토큰 획득
```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{
    "username": "testuser",
    "password": "user123"
  }'
```

#### 3. 파일 암호화 (JWT 토큰 필요)
```bash
curl -X POST http://localhost:8080/api/files/encrypt \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -F "file=@/path/to/your/file.txt" \
  --output encrypted_file.zip
```

#### 4. 웹 취약점 분석
```bash
curl -X POST http://localhost:8080/api/analysis/start \
  -H "Authorization: Bearer YOUR_JWT_TOKEN" \
  -H "Content-Type: application/json" \
  -d '{
    "url": "https://example.com"
  }'
```

### Flask AI 서버 실행 확인

웹 취약점 분석 기능을 사용하려면 Flask 서버가 실행 중이어야 합니다.

```bash
# Flask 서버 상태 확인
curl http://127.0.0.1:5001/health

# Flask 서버가 없다면 별도로 실행 필요
# (Flask 프로젝트는 별도 저장소에 위치)
```

-----

## ⚠️ 주의사항

### 보안 설정
* **JWT Secret**: `application.yml`의 `app.jwt.secret`을 256비트 랜덤 문자열로 변경하세요.
* **암호화 마스터 키**: `app.encryption.master-key`를 256비트 랜덤 문자열로 변경하세요.
* **DB 비밀번호**: `spring.datasource.password`를 강력한 비밀번호로 변경하세요.
* **Gmail 앱 비밀번호**: `spring.mail.password`에 실제 Gmail 앱 비밀번호를 설정하세요.

### 파일 관리
* 로컬 환경에서 실행 시, `application-example.yml`을 복사하여 `application.yml`을 생성하세요.
* `application.yml` 파일은 `.gitignore`에 포함되어 Git에 커밋되지 않습니다.
* 암호화된 파일은 `./data/files/encrypted/uploads/` 디렉토리에 저장됩니다.

### 운영 환경
* 운영 환경에서는 환경 변수 또는 외부 설정 서버(Spring Cloud Config)를 사용하세요.
* HTTPS를 사용하여 통신을 암호화하세요.
* CORS 설정(`allowed-origins`)을 프로덕션 도메인으로 제한하세요.

-----

## 📞 문의 및 지원

- **Email**: devhyxuk@gmail.com
- **GitHub**: [ShieldHub Backend Repository](https://github.com/yourusername/shieldhub-backend)

-----

## 📄 라이선스

이 프로젝트는 MIT 라이선스 하에 배포됩니다.

-----

<div align="center">

**🛡️ ShieldHub - 당신의 데이터를 안전하게 보호합니다 🛡️**

Made with ❤️ by ShieldHub Team

</div>
